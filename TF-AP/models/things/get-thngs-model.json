{
	"<deviceId>":{ // name of the thng
		"id":"<STRING>", // Can be the thing ID 
		"name":"<STRING>", // machine-readable name for that thng (shall be used in URLs), e.g. "livinghue2"
		"description":"<STRING>", // human readable description, e.g. "Living Room Hue Lamp #2"
		"url":"<STRING>" // The root URL of that thing - MUST be a WT, can be Absolute (http://...) or relative (/thngs/X), to which we always append the "gateway" root url
		// customFields & tags? I'd say yes. 
		// links, productId, 
	}//,...	
}